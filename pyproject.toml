[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "alfbot"
version = "0.1.0"
description = "ALF bots"
dependencies = ["coloralf", "python-telegram-bot"]

[tool.setuptools.packages.find]
where = ["."]